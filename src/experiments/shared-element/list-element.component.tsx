import React from 'react';
import { FlatList, Image } from 'react-native';
import { DetailElement, Element } from './detail-element.component';

export type ListElementProps = {
  elements: Element[]
};

export const ListElement: React.FunctionComponent<ListElementProps> = ({ elements }) => (

);